{
    "adapters": 1,
    "builtin": false,
    "category": "guest",
    "symbol": ":/symbols/classic/server-sdn.svg",
    "compute_id": "local",
    "template_id": "ec2f9f5b-861b-418f-96a0-5476f85318e0",
    "image": "cqugns3/faucetnode:latest",
    "template_type": "docker",
    "name": "SDN Controller",
    "usage": "Faucet SDN controller. Edit /etc/faucet/faucet.yaml — that one file configures every switch the controller manages — then run: start-faucet.sh (it validates the file first and will not start on a bad one). Check it with faucet-status.sh and watch switches connect with tail -f /var/log/faucet/faucet.log. Example configurations are in /etc/faucet/examples/. On the switch: ovs-vsctl set bridge br0 other-config:datapath-id=0000000000000001 and ovs-vsctl set-controller br0 tcp:<this-node-ip>:6653 — the datapath-id must match a dp_id in faucet.yaml. Metrics for the Monitor node are on tcp/9302.",
    "mac_address": "",
    "start_command": "",
    "environment": "",
    "console_type": "telnet",
    "console_auto_start": false,
    "console_http_port": 80,
    "console_http_path": "/",
    "console_resolution": "1024x768",
    "extra_hosts": "",
    "extra_volumes": ["/etc", "/root", "/usr/local/bin"],
    "custom_adapters": [],
    "default_name_format": "SDN{0}"
}
