{
    "adapters": 1,
    "builtin": false,
    "category": "guest",
    "symbol": ":/symbols/classic/computer-dns.svg",
    "compute_id": "local",
    "template_id": "6512c51d-e014-454b-bc12-f339e4022a68",
    "image": "cqugns3/servicenode:latest",
    "template_type": "docker",
    "name": "DNS Server",
    "usage": "BIND DNS server, authoritative and recursive. Start with: start-dns.sh (it runs named-checkconf first, so a bad zone file is reported rather than silently ignored). Edit /etc/bind/named.conf and the zone files in /var/bind/pri/ — an example.com forward zone and a 192.168.1.0/24 reverse zone ship ready to answer. Remember to increase the zone serial after every edit. Check what it is doing with: dns-status.sh",
    "mac_address": "",
    "start_command": "",
    "environment": "",
    "console_type": "telnet",
    "console_auto_start": false,
    "console_http_port": 80,
    "console_http_path": "/",
    "console_resolution": "1024x768",
    "extra_hosts": "",
    "extra_volumes": ["/etc", "/root", "/usr/local/bin", "/var/bind"],
    "custom_adapters": [],
    "default_name_format": "DNS{0}"
}
