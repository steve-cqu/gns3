{
    "adapters": 1,
    "builtin": false,
    "category": "guest",
    "symbol": ":/symbols/classic/server-dhcp.svg",
    "compute_id": "local",
    "template_id": "02cf5388-0176-4ca8-a72a-69d0d461ec0e",
    "image": "cqugns3/servicenode:latest",
    "template_type": "docker",
    "name": "DHCP Server",
    "usage": "Kea DHCPv4 server (ISC's modern successor to the end-of-life isc-dhcp-server). Start with: start-dhcp.sh (it runs kea-dhcp4 -t first, so a bad config is reported rather than silently ignored). It ships serving 192.168.1.100-200 on 192.168.1.0/24; edit /etc/kea/kea-dhcp4.conf to change the pool, add options or add a reservation. On a client node request a lease with: udhcpc -i eth0. Leases persist in /var/lib/kea. See what it has handed out with: dhcp-status.sh",
    "mac_address": "",
    "start_command": "",
    "environment": "",
    "console_type": "telnet",
    "console_auto_start": false,
    "console_http_port": 80,
    "console_http_path": "/",
    "console_resolution": "1024x768",
    "extra_hosts": "",
    "extra_volumes": ["/etc", "/root", "/usr/local/bin", "/var/lib/kea"],
    "custom_adapters": [],
    "default_name_format": "DHCP{0}"
}
