{
    "adapters": 1,
    "builtin": false,
    "category": "guest",
    "symbol": ":/symbols/classic/computer-monitor.svg",
    "compute_id": "local",
    "template_id": "95e22a5d-bd5b-4577-9a69-4470ba4d0a73",
    "image": "cqugns3/monitornode:latest",
    "template_type": "docker",
    "name": "Monitor",
    "usage": "Prometheus, Grafana and blackbox_exporter on one node. Start them one at a time the first time — start-blackbox.sh, start-prometheus.sh, start-grafana.sh — or all at once with start-monitoring.sh. Check everything with monitor-status.sh. Grafana is at http://<this-node-ip>:3000, admin / gns3, reached from a Firefox Host node in the same topology; add Prometheus as a data source at http://localhost:9090. Prometheus scrapes itself out of the box, so you can confirm the stack works before writing a scrape job. Edit /etc/prometheus/prometheus.yml and check it with promtool check config. Dashboards persist in /var/lib/grafana.",
    "mac_address": "",
    "start_command": "",
    "environment": "",
    "console_type": "telnet",
    "console_auto_start": false,
    "console_http_port": 80,
    "console_http_path": "/",
    "console_resolution": "1024x768",
    "extra_hosts": "",
    "extra_volumes": ["/etc", "/root", "/usr/local/bin", "/var/lib/grafana", "/var/lib/prometheus"],
    "custom_adapters": [],
    "default_name_format": "Monitor{0}"
}
