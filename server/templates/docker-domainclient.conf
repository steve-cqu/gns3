{
    "adapters": 1,
    "builtin": false,
    "category": "guest",
    "symbol": ":/symbols/classic/computer-domain.svg",
    "compute_id": "local",
    "template_id": "b73748c9-23ad-4a1c-ab09-4054ec42ea8f",
    "image": "cqugns3/ubuntunode:latest",
    "template_type": "docker",
    "name": "Domain Client",
    "usage": "Ubuntu Host with the Active Directory join tooling already installed — realmd, sssd, adcli, kinit/klist and ldapsearch. Point /etc/resolv.conf at the Domain Controller first (a client that cannot resolve the domain's SRV records cannot find it, and the error will not say so), then: realm discover ad.example.com, realm join -U administrator ad.example.com, and log in as a domain user with su - alice@ad.example.com. Check membership with realm list and id alice@ad.example.com. Same image as the Ubuntu Host — the difference is the packages, the name and this note.",
    "mac_address": "",
    "start_command": "",
    "environment": "",
    "console_type": "telnet",
    "console_auto_start": false,
    "console_http_port": 80,
    "console_http_path": "/",
    "console_resolution": "1024x768",
    "extra_hosts": "",
    "extra_volumes": ["/etc", "/root", "/home", "/var/www", "/usr/local/bin"],
    "custom_adapters": [],
    "default_name_format": "Client{0}"
}
