        {
            "adapters": 4,
            "builtin": false,
            "category": "guest",
            "symbol": ":/symbols/classic/router-vpn.svg",
            "compute_id": "local",
            "template_id": "e8bb159e-a899-ed9c-702d-f7407dbfb5e2",
            "image": "cqugns3/alpinenode:latest",
            "template_type": "docker",
            "name": "VPN Router",
            "usage": "Linux router with Tailscale, for joining your GNS3 project to another student's over the Internet. eth0 is the LAN side - give it the .1 address of your group's agreed range; eth1 goes to a GNS3 NAT node and gets its address by DHCP. Join the mesh with: start-tailscale.sh --key -  (it checks eth1 really has Internet and can reach the coordination server, works out the /16 to advertise from eth0's address, starts the daemon, and tells you if your route has not been approved). Try start-tailscale.sh --check first to test everything without joining, or --explain to see the raw tailscale commands it would run. Check it with: tailscale-status.sh - section 3 says whether the tailnet owner has approved your route, which is the step groups most often miss. Leave the mesh with: stop-tailscale.sh. This node never joins by itself and nothing about the join survives closing the project: being on the mesh is something you do on purpose, each session.",
            "mac_address": "",
            "start_command": "",
            "environment": "",
            "console_type": "telnet",
            "console_auto_start": false,
            "console_http_port": 80,
            "console_http_path": "/",
            "console_resolution": "1024x768",
            "extra_hosts": "",
            "extra_volumes": ["/etc", "/root", "/var/www", "/usr/local/bin"],
            "custom_adapters": [],
            "default_name_format": "VPNRouter{0}"
        }
        